# Root environment variables (optional)
# Copy to .env.local for local overrides

# Per-app env vars live in each app: see apps/frontend/.env.example (incl.
# GOOGLE_MAPS_API_KEY for device-location reverse geocoding) and
# apps/backend/.env.example.